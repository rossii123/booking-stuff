# Minut take-home assignment (fullstack 2023)

## Quickstart

To get this service running locally, you need nodejs, npm and docker installed. Then run these commands

```bash
npm install
docker-compose up --build -d
curl localhost:5006 # Should print Hello World!
```

You're free to modify the project setup however you like, if you have a preferred way of working. 
